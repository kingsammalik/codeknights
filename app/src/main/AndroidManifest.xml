<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.root.codeknights">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DashBoardFarmer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".otpverification" />
        <activity android:name=".fivedayforecast" />
        <activity android:name=".Recommendation" />
        <activity android:name=".Displaycrop" />
        <activity android:name=".crop_yield_calculator" />
        <activity android:name=".report" />
        <activity android:name=".SlideImageText" />
        <activity android:name=".Dashboard"/>
        <activity android:name=".LogIn" />
        <activity android:name=".LogInInsurance" />
        <activity android:name=".CropInfoActivity" />

    </application>

</manifest>